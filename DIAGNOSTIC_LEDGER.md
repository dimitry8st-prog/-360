# Диагностический журнал

## Целевой результат

Рабочий пользовательский сценарий: регистрация → загрузка файла → анализ → интерактивный график → история → админ-журнал.

## Подтверждённое окружение

- Python 3.12.13 доступен.
- Репозиторий назначения был пуст на момент начала работы.
- Docker в текущей среде не установлен; контейнерный запуск здесь не заявляется как проверенный.

## Границы проверки

| Граница | Сигнал успеха |
|---|---|
| Приложение | `GET /health` возвращает 200 |
| База | `/health` сообщает `database=ok` |
| Авторизация | регистрация создаёт сессию и открывает кабинет |
| WebSocket | авторизованный клиент получает `connected` |
| Файл | CSV сохраняется и получает проверяемую сводку |
| График | создаётся интерактивный HTML и, при доступном движке, PNG |
| Docker | все сервисы healthy — требуется Docker-хост |

## Безопасные заглушки

- OpenAI без ключа работает в режиме `demo_stub`.
- SMTP и 2FA обозначены как незавершённые и не имитируют реальную отправку.
- AI-песочница выключена до отдельного security-теста контейнерной изоляции.

